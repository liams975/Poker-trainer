// Phase 0 ships no product code.
//
// The module map in docs/03-poker-engine.md (rng, cards, evaluator, equity,
// ranges, game, strategy, drills) is created in Phase 1 onward, as each module
// is actually written and tested. Eight empty barrel files would be exactly the
// sprawl docs/06-claude-code-workflow.md warns against.
export {};
